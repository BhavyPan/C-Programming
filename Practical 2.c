#include<stdio.h>

void main()

    {
        int length,breadth,Area,Perimeter;
        length=70;
        breadth=90;
        Area=length*breadth;
        Perimeter=2*(length+breadth);

        printf("Total Area is %d \n" ,Area);
        printf("Total Perimeter is %d \n",Perimeter);
    }
